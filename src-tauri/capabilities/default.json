{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the main window can call explicitly registered Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-set-size"]
}
